/target
**/*.rs.bk
*.pdb

# Tauri / frontend (added in a later milestone)
node_modules/
dist/

# Local test data: raw frames are large and not ours to redistribute
/testdata
/samples

# Stack output: hundreds of megabytes per run, and reproducible from the frames
/out
/out-*

# Release staging from build.bat
/build

# Every built version, kept so an old one can be run beside a new one.
# Twenty megabytes a build, and binaries have no business in the history.
/archive

# Desktop shell
/apps/desktop/node_modules
/apps/desktop/dist
/apps/desktop/src-tauri/target
/apps/desktop/src-tauri/gen

# Python-мусор от скриптов в tools/
__pycache__/
